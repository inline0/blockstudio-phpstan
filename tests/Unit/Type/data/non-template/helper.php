<?php

declare(strict_types=1);

use function PHPStan\Testing\assertType;

assertType('mixed', $a);
assertType('mixed', $attributes);
assertType('mixed', $b);
assertType('mixed', $content);
assertType('mixed', $isEditor);
